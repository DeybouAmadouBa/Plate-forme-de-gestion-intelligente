<?php
require 'db.php';
require 'auth.php';
checkRole('prof');

if(isset($_POST['submit'])){
    $titre = $_POST['titre'];
    $lien = $_POST['lien'];
    $prof_id = $_SESSION['user_id'];

    $conn->query("INSERT INTO live_cours(titre,lien,professeur_id)
                  VALUES('$titre','$lien','$prof_id')");
}
?>

<form method="POST">
    <input type="text" name="titre" placeholder="Titre du cours"><br>
    <input type="text" name="lien" placeholder="Lien Google Meet"><br>
    <button name="submit">Créer Live</button>
</form>
